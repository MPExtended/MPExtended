<?xml version="1.0" encoding="utf-8"?>
<serviceconfig>
  <users authenticationEnabled="true">
    <user>
      <username>admin</username>
      <password>VF4iJh8Ccd//JDuWSygi/A==</password>
    </user>
  </users>

  <!-- Use this to use a different user for accessing network shares -->
  <networkImpersonation>
    <domain></domain>
    <username></username>
    <password></password><!-- This is encrypted, so set it only through the configurator -->
    <readInStreamingService>true</readInStreamingService>
  </networkImpersonation>

  <bonjour>
    <enabled>true</enabled>
    <pcname /><!-- Automatically detected when empty -->
  </bonjour>

  <accessrequest>
    <enabled>true</enabled>
  </accessrequest>

  <port>4322</port>
  <enableIPv6>false</enableIPv6>

  <!-- If you've have multiseat setup you can specify different hosts for the other host here. This is picked up by WSS (but it doesn't work) and by WebMP. -->
  <connections>
    <mas>auto://127.0.0.1:4322</mas>
    <tas>auto://127.0.0.1:4322</tas>
  </connections>
</serviceconfig>