@model MPExtended.Applications.WebMediaPortal.Models.PlayerViewModel

@using (Html.BeginForm())
{
    @Html.DropDownList("transcoder", Model.TranscoderSelectList)
    <input type="submit" value="Change player" />
}

<br />
@Html.ActionLink(UIStrings.DownloadPlaylist, "Playlist", new { type = Model.MediaType, itemId = Model.MediaId })

<br />
<br />
@Html.Partial(Model.PlayerViewName)